{
  "name": "node-templer",
  "version": "0.0.1",
  "description": "template engine for projects.",
  "bin": "./bin/index.js",
  "scripts": {
    "start": "npm run build && npm uninstall && npm link",
    "build": "./node_modules/.bin/babel ./src --out-dir ./bin/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "template",
    "project"
  ],
  "author": "StTraft",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.3.3",
    "@babel/preset-env": "^7.3.1",
    "babel-preset-minify": "^0.5.0"
  },
  "dependencies": {
    "chalk": "^2.4.2",
    "figlet": "^1.2.1",
    "inquirer": "^6.2.2",
    "rimraf": "^2.6.3",
    "yargs": "^13.2.1"
  }
}
